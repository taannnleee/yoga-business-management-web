export interface Variants {
  size?: Record<string, string>; // size variants with size names as keys and image URLs as values
  color?: Record<string, string>; // color variants with color names as keys and image URLs as values
}
